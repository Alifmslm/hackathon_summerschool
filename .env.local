# Local environment variables (not committed in real projects).
# Add keys here as the project grows (e.g. AI provider keys, PDF service).

# NEXT_PUBLIC_APP_NAME="IT Career Navigator"

# Optional: enables AI-generated project recommendations (falls back to the
# static template pool when unset). Get a key at https://aistudio.google.com/apikey
# Set GEMINI_API_KEY in your untracked .env (see .gitignore) — do not commit it here.
